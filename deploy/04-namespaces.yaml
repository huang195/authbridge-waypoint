---
# agent-ns: ambient mode enabled
apiVersion: v1
kind: Namespace
metadata:
  name: agent-ns
  labels:
    istio.io/dataplane-mode: ambient
---
# tool-ns: ambient mode only (no waypoint — security is on the agent side)
apiVersion: v1
kind: Namespace
metadata:
  name: tool-ns
  labels:
    istio.io/dataplane-mode: ambient
---
# kagenti-system: where the shared token-exchange-service runs
apiVersion: v1
kind: Namespace
metadata:
  name: kagenti-system
  labels:
    istio.io/dataplane-mode: ambient
